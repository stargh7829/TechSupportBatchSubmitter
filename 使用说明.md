# 技术支持批量提交工具使用说明

版本：1.4.3

本文给下载和实际操作人员使用。无需了解代码，也不需要安装 Visual Studio。

## 一、下载和启动

1. 下载便携版 ZIP，文件名通常是 `技术支持批量提交工具-win-x64.zip`。
2. 右键 ZIP 解压到一个固定目录，例如桌面或工作文件夹。
3. 双击解压目录里的 `TechSupportBatchSubmitter.exe`。
4. 如提示缺少 Microsoft Edge WebView2 Runtime，运行同目录下的 `MicrosoftEdgeWebview2Setup.exe`。

注意：GitHub 页面里的 `Code`、`Issues`、`Pull requests` 等英文按钮是 GitHub 网站自己的功能，
不是本软件的操作按钮。普通使用人员只需要下载 ZIP 并运行 EXE。

## 二、登录平台

1. 软件左侧是业务系统登录页面。
2. 在左侧页面完成登录。
3. 登录后进入技术支持系统。

软件不保存账号、密码、Cookie，也不提供模拟登录页。

## 三、批量提交技术支持

1. 在“批量提交”页点击“选择并读取”。
2. 选择技术支持 Excel 清单，程序会自动读取并在原文件同目录创建备份。
3. 查看模板校验摘要；如出现“校验失败”，先按失败原因修正 Excel。
4. 点击“开始提交”。
5. 程序会先校验平台字段，确认无误后再开始提交。
6. 第一条会立即提交；后续默认随机等待 30-90 秒。
7. 如果填写“提交间隔”，则固定按填写的秒数等待，不再使用随机区间。

事件申请创建核验成功后，程序会立即打开该办件的“受理并处理”表单，按本行“处理说明”提交并回读状态。只有这一步也核验成功，记录才显示提交成功。

程序会把“技术支持编号、提交状态、实际提交时间、失败原因、受理并处理状态、受理并处理完成时间、受理并处理失败原因”等结果写回 Excel。

导入表最后一列“处理类型”填写平台下拉值“运营”或“运维”。程序会按该值提交；旧版模板未有此列时会自动补齐为“运营”。

“处理说明”为必填列，用于每条办件的“受理并处理”。缺失时，该行会在预检阶段标记为校验失败。若受理并处理结果不明确，程序会保留原技术支持编号并标记为待核验，不会自动重复受理。

## 四、待受理查询和关闭

1. 切换到“待受理查询”页。
2. 默认只按“事件标题”查询。
3. 需要更多条件时，点击“更多条件”展开。
4. 查询结果中可以单条点击“关闭”，也可以勾选多条后点击“批量关闭”。
5. 批量关闭第一条立即执行；后续默认随机等待 5-10 秒。
6. 如果填写“关贴间隔”，则固定按填写的秒数等待。

默认按当前工具内置字段执行“受理并总结”，核验进入“已解决”后视为成功。
如需特殊回复口径，点击“自定义回复”展开配置区，选择原因分类树并填写原因描述、
解决方法和解决方案；未展开时仍使用默认回复。

## 五、提交清单关闭

1. 先在“批量提交”页选择当前 Excel 清单。
2. 切换到“提交清单关闭”页。
3. 点击“查询清单”，程序会按 Excel 中已回填的技术支持编号生成关闭列表。
4. 单条关闭或勾选后批量关闭。
5. 关闭结果会写回同一个 Excel 的“关闭状态、实际关闭时间、关闭失败原因”列。

## 六、历史记录

切换到“历史记录”页，可以查看本机提交历史和关闭历史。

历史数据保存在本机：

`%LocalAppData%\TechSupportBatchSubmitter`

历史中包含编号、标题、状态、Excel 行号、来源和处理结果；不保存事件描述、密码、Cookie、
身份证号或手机号。

## 七、导出诊断包

如果程序异常、登录状态无法判断、Excel 回写失败或平台结构变化，可在“运行日志”区域点击“导出诊断包”。

诊断包包含程序版本、运行环境、当前 Excel 摘要和脱敏日志；不包含 Excel 原文件、工单描述、账号密码、Cookie 或 Token。

## 八、常见问题

### 1. 为什么 GitHub 页面是英文？

GitHub 网站的菜单和按钮由 GitHub 控制，可能显示英文。软件说明已经放在仓库首页和本文档里，
实际操作人员按中文说明下载 ZIP、运行 EXE 即可。

### 2. 可以直接下载 Source code 吗？

不建议普通使用人员下载 Source code。Source code 是源码包，主要给开发人员使用。
普通使用人员应下载便携版 ZIP。

### 3. 提交期间能打开 Excel 吗？

不要打开或修改同一个 Excel。Excel 被占用时，程序可能无法写回提交结果或关闭结果。

### 4. 已有技术支持编号的行会怎样？

已有技术支持编号的行会自动跳过，不会重复提交。

### 5. 出现“待核验”怎么办？

“待核验”表示程序无法确定平台保存结果。需要先到平台人工核验，程序不会自动换新编号重复提交。

### 6. 登录失效怎么办？

在左侧登录页面重新登录并进入技术支持系统，然后重新执行当前操作。

## 九、注意事项

- Excel 原文件同目录会自动生成时间戳备份。
- 关闭保存结果不明确时，队列会暂停，避免重复关闭。
- 当前版本不处理附件。
- 当前版本不处理平台独立的二次关闭步骤。
- 程序只对目标工作台和技术支持系统两个固定地址放行内部证书错误。
